# ⚡ دليل البدء السريع - Quick Start Guide

## 📋 ملخص النظام

نظام متكامل لحجز مواعيد عرض القرآن الكريم:
- 🕌 الشيخ يضبط توفريته
- 📅 الطالب يحجز موعد
- 🔔 إشعارات تلقائية
- 📊 إحصائيات وتقارير

---

## 📁 الملفات الرئيسية

```
src/
├── types/index.ts                          # الأنواع الجديدة
├── services/
│   ├── appointmentService.ts              # منطق المواعيد
│   └── notificationService.ts             # الإشعارات
└── app/
    ├── appointments/page.tsx               # لوحة الشيخ
    └── book-appointment/page.tsx           # صفحة حجز الطالب
```

---

## 🎯 الميزات الرئيسية

### 1️⃣ جدول المواعيد (Sheikh Dashboard)
```
الشيخ يقوم بـ:
✓ إضافة فترات زمنية لكل يوم
✓ اختيار المكان (مسجد، مصلى، مدرسة)
✓ تحديد السعة (عدد الطلاب)
✓ تأكيد/رفض الحجوزات
✓ عرض الإحصائيات
```

### 2️⃣ صفحة الحجز (Student Booking)
```
الطالب يقوم بـ:
✓ اختيار التاريخ
✓ اختيار الوقت المتاح
✓ اختيار السورة المراد عرضها
✓ إضافة ملاحظات
✓ تأكيد الحجز
```

### 3️⃣ الإشعارات التلقائية
```
النظام يرسل:
✓ تأكيد الحجز
✓ تنبيهات قبل الموعد
✓ إشعارات الإلغاء
✓ تقييم بعد الموعد
```

---

## 🚀 البدء السريع

### للشيخ:
```typescript
// 1. إضافة جدول المواعيد
import { createTimeSlot, createDayAvailability } from "@/services/appointmentService";

const slot = createTimeSlot(
  "15:00",           // وقت البداية
  "17:00",           // وقت الانتهاء
  "mosque",          // المكان
  "المسجد الكبير",  // التفاصيل
  1                  // السعة
);

const daySchedule = createDayAvailability(6, [slot]); // السبت (6)
```

### للطالب:
```typescript
// 2. حجز موعد
import { bookAppointment } from "@/services/appointmentService";

const appointment = bookAppointment(
  "student-id",
  "أحمد محمد",
  "ahmad@email.com",
  "sheikh-id",
  "2026-02-15",      // التاريخ
  timeSlot,          // الفترة المختارة
  "المسجد",
  undefined,
  26,                // معرّف السورة (الشعراء)
  "Ash-Shu'ara",
  "الشعراء",
  "1-50"             // نطاق الآيات
);
```

### تأكيد الحجز:
```typescript
import { confirmAppointment } from "@/services/appointmentService";

const confirmed = confirmAppointment(appointment);
// الآن status = "confirmed"
```

---

## 📊 معادلة الفترات الزمنية

```
فترة واحدة: 15 دقيقة أو أكثر

مثال: من 15:00 إلى 17:00 (ساعتان)
↓
✓ 15:00 - 15:15 (ربع ساعة)
✓ 15:15 - 15:30 (ربع ساعة)
✓ 15:30 - 15:45 (ربع ساعة)
✓ 15:45 - 16:00 (ربع ساعة)
✓ ... وهكذا حتى 17:00
```

---

## 🔄 حالات الحجز

```
┌─────────────┐
│   pending   │ ← الحجز الجديد (ينتظر تأكيد الشيخ)
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ confirmed   │ ← مؤكد (مرسل إشعار للطالب)
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ completed   │ ← مكتمل (بعد الموعد)
└─────────────┘

أو في أي وقت:
┌─────────────┐
│ cancelled   │ ← ملغى (من الطالب أو الشيخ)
└─────────────┘
```

---

## 📱 واجهات المستخدم

### صفحة الشيخ: `/appointments`
```
📅 جدول المواعيد | 🕐 الحجوزات | 📊 الإحصائيات

[اختيار اليوم]
- السبت
- الأحد
- ...

[الفترات الزمنية]
15:00 - 17:00 | المسجد | 0/1

[إضافة فترة جديدة]
وقت البداية: [___]
وقت الانتهاء: [___]
المكان: [▼]
السعة: [_]
```

### صفحة الطالب: `/book-appointment`
```
Step 1: اختيار التاريخ
□ الأحد 14 فبراير (5 فترات متاحة)
□ الاثنين 15 فبراير (3 فترات متاحة)

Step 2: اختيار الوقت
[15:00 - 15:15] المسجد
[15:30 - 15:45] المسجد ✓ (مختار)

Step 3: التأكيد
السورة: [الشعراء ▼]
ملاحظات: ______
[تأكيد الحجز]
```

---

## 🔍 الدوال الأساسية

### إنشاء وإدارة الفترات

```typescript
// إنشاء فترة زمنية
createTimeSlot(startTime, endTime, location, details, capacity)

// إنشاء توفرية يومية
createDayAvailability(dayOfWeek, timeSlots)

// إنشاء جدول أسبوعي كامل
createWeeklySchedule(availability)
```

### إدارة الحجوزات

```typescript
// حجز جديد
bookAppointment(...details)

// تأكيد الحجز
confirmAppointment(appointment)

// إلغاء الحجز
cancelAppointment(appointment, reason)

// إكمال الموعد
completeAppointment(appointment, rating, feedback)

// إعادة جدولة
rescheduleAppointment(appointment, newDate, newSlot)
```

### الإحصائيات

```typescript
// إحصائيات يوم معين
getDayStatistics(date, appointments, slots)

// إحصائيات الشيخ الشاملة
calculateSheikhStatistics(appointments, slots)

// الفترات المتاحة
getAvailableSlots(date, dayOfWeek, availability, appointments)
```

### الإشعارات

```typescript
// إشعار تأكيد الحجز
createBookingConfirmationNotification(...)

// إشعار الإلغاء
createCancellationNotification(...)

// تذكير قبل الموعد
createReminderNotification(...)

// طلب إعادة جدولة
createRescheduleRequestNotification(...)
```

---

## 🛡️ التحقق والأمان

```typescript
// التحقق من صيغة الوقت
isValidTimeFormat(time)  // "15:00" ✓, "25:00" ✗

// التحقق من الفترة الزمنية
isTimeSlotAvailable(slot, appointments, date)

// البحث عن التضارب
findStudentConflicts(studentId, appointments, date, start, end)

// التحقق من انتهاء الصلاحية
isNotificationExpired(notification)
```

---

## 📈 الإحصائيات المتاحة

```typescript
{
  total: 24,              // إجمالي المواعيد
  completed: 18,          // المكتملة
  cancelled: 2,           // الملغاة
  pending: 4,             // قيد الانتظار
  averageRating: 4.8,     // متوسط التقييم
  bookingRate: 75         // نسبة الحجز
}
```

---

## 🔗 التكامل مع Firebase

```typescript
// (يتم إضافته لاحقاً)
import { db } from "@/lib/firebase";
import { collection, addDoc, updateDoc } from "firebase/firestore";

// حفظ الموعد
const appointmentsRef = collection(db, "appointments");
await addDoc(appointmentsRef, appointment);

// تحديث الموعد
await updateDoc(appointmentDoc, { status: "confirmed" });
```

---

## 📞 الدعم والمشاكل الشائعة

### المشكلة: "لا توجد فترات متاحة"
```
الحل:
1. تحقق من أن الشيخ أضاف فترات لهذا اليوم
2. تحقق من أن الفترات لم تنتهِ سعتها
3. تأكد من عدم وجود تضارب زمني
```

### المشكلة: "لا يمكن الحجز لتاريخ ماضي"
```
الحل:
1. اختر تاريخاً في المستقبل فقط
2. اختر تاريخاً يوجد فيه فترات متاحة
```

### المشكلة: "خطأ في حفظ الحجز"
```
الحل:
1. تحقق من الاتصال بالإنترنت
2. تحقق من صحة بيانات الإدخال
3. تأكد من أن معرّفات السوراه صحيحة
```

---

## 🎓 أمثلة عملية

### مثال 1: الشيخ يضبط مواعيده

```typescript
// اليوم: السبت (6)
const morningSlot = createTimeSlot("09:00", "12:00", "mosque");
const eveningSlot = createTimeSlot("15:00", "18:00", "prayer_room");

const saturdaySchedule = createDayAvailability(6, [
  morningSlot,
  eveningSlot,
]);

// النتيجة:
// صباح: 9:00 - 12:00 بالمسجد
// مساء: 3:00 - 6:00 بالمصلى
```

### مثال 2: طالب يحجز موعداً

```typescript
const appointment = bookAppointment(
  "student123",
  "محمد أحمد",
  "mohammad@example.com",
  "sheikh456",
  "2026-02-14",      // السبت
  morningSlot,
  "المسجد الكبير",
  undefined,
  2,                 // البقرة
  "Al-Baqarah",
  "البقرة",
  "1-50",
  "أنا حفظت من الآية 1 إلى 50"
);

// status: "pending"
// ينتظر تأكيد الشيخ
```

### مثال 3: الشيخ يؤكد الحجز

```typescript
const confirmed = confirmAppointment(appointment);
// يتم إرسال إشعار للطالب تلقائياً
// status: "confirmed"
```

---

## ✅ قائمة الفحص

```
Before Deployment:
□ جميع الأنواع معرّفة بشكل صحيح
□ الخدمات تعمل بشكل صحيح
□ الواجهات تعرض البيانات بشكل صحيح
□ الإشعارات تُرسل تلقائياً
□ الإحصائيات دقيقة
□ لا توجد أخطاء في وحدة التحكم

Testing:
□ اختبارات الوحدة تمر
□ اختبارات التكامل تمر
□ لا توجد أخطاء TypeScript
□ لا توجد تحذيرات ESLint
```

---

**جاهز للاستخدام! 🚀**

للمزيد من التفاصيل، راجع `APPOINTMENT_SYSTEM_GUIDE.md`
