# 📅 نظام التقويم والأوراد - Wird Calendar System

> نظام متكامل لإدارة الأوراد اليومية ومتابعة تقدم الطلاب على مستوى كل ورد

---

## 🎯 الميزات الرئيسية

### 1. **صفحة التقويم** (`/calendar`)
- تقويم تفاعلي شهري مع جميع أيام السنة
- تحديد الورد لكل يوم (سورة + أيات)
- تحديد أيام العطل بسهولة
- إضافة ملاحظات لكل ورد
- حذف وتعديل الأوراد
- عرض إحصائيات الشهر (عدد الأوراد، أيام العطل)

### 2. **صفحة متابعة الأوراد** (`/wird-tracking`)
- عرض جميع الأوراد المضافة
- أعمدة بيانية لتقدم كل طالب في كل ورد
- إحصائيات مفصلة:
  - معدل الإكمال الإجمالي
  - عدد الطلاب المكملين
  - الطلاب قيد الإنجاز
  - الطلاب الذين لم يبدأوا
- جدول تفصيلي بتقدم كل طالب
- تتبع النسب المئوية والحالات

---

## 🏗️ البنية التقنية

### أنواع البيانات الجديدة

#### 1. `SurahInfo`
```typescript
interface SurahInfo {
  id: number;
  name: string;
  arabicName: string;
  totalAyahs: number;
}
```

#### 2. `WirdAssignment` (محدّث)
```typescript
interface WirdAssignment {
  id?: string;
  date: string; // YYYY-MM-DD
  dayOfWeek: string;
  surahId: number;
  surahName: string;
  arabicSurahName: string;
  startAyah: number;
  endAyah: number;
  totalAyahs: number;
  targetRepetitions: number; // عدد التكرارات المطلوب
  isHoliday: boolean;
  note?: string;
  createdAt?: number;
  updatedAt?: number;
}
```

#### 3. `StudentProgress` (محدّث)
```typescript
interface StudentProgress {
  date: string;
  wirdId: string; // معرّف الورد
  wirdName: string;
  count: number; // التكرارات المكتملة
  targetCount: number; // الهدف
  timestamp: number;
  status: "pending" | "in-progress" | "completed";
  completedAt?: number;
  notes?: string;
}
```

#### 4. `WirdTracking` (جديد)
```typescript
interface WirdTracking {
  wirdId: string;
  date: string;
  surah: string;
  ayahRange: string;
  targetRepetitions: number;
  studentProgress: Record<string, number>;
  statistics: {
    completed: number;
    inProgress: number;
    notStarted: number;
    averageCompletion: number;
  };
}
```

---

## 📂 الملفات الجديدة

### الخدمات
- **`src/services/calendarService.ts`** (400+ سطر)
  - إدارة التقويم والأوراد
  - معالجة تاريخ وأيام الأسبوع
  - التحقق من صحة الآيات
  - معلومات جميع السور

### الـ Hooks
- **`src/hooks/useWirdCalendar.tsx`** (300+ سطر)
  - `useWirdCalendar`: إدارة الأوراد
  - `useWirdProgress`: متابعة تقدم الطلاب

### الصفحات
- **`src/app/calendar/page.tsx`** (450+ سطر)
  - صفحة التقويم التفاعلي
  - نموذج إضافة الأوراد
  - عرض الإحصائيات الشهرية

- **`src/app/wird-tracking/page.tsx`** (350+ سطر)
  - صفحة متابعة الأوراد
  - أعمدة بيانية للتقدم
  - جداول التفاصيل

---

## 🔧 كيفية الاستخدام

### للشيخ: إضافة ورد جديد

1. **الذهاب لصفحة التقويم** (`/calendar`)
   ```
   الشريط الجانبي → التقويم
   ```

2. **اختيار اليوم** 
   - انقر على أي يوم في التقويم

3. **ملء النموذج**
   ```
   - اختر السورة (من 30 سورة)
   - حدد آية البداية
   - حدد آية النهاية
   - عدد التكرارات المطلوب
   - أضف ملاحظات (اختياري)
   ```

4. **حفظ الورد**
   - انقر "إضافة"

### مثال عملي

```
اليوم: الإثنين 3 فبراير 2026
السورة: الشعراء
من الآية: 1
إلى الآية: 10
عدد التكرارات: 5
ملاحظات: "تركيز على الحروف المشددة"
```

النتيجة: `سورة الشعراء 1-10` سيظهر في التقويم

---

## 📊 متابعة التقدم

### للشيخ: عرض تقدم الأوراد

1. **الذهاب لصفحة متابعة الأوراد** (`/wird-tracking`)
   ```
   الشريط الجانبي → الأوراد
   ```

2. **اختيار ورد**
   - انقر على أي ورد من القائمة أعلى

3. **عرض البيانات**
   - أعمدة بيانية بتقدم كل طالب
   - معدل الإكمال الكلي
   - عدد المكملين والمتأخرين

---

## 🎨 واجهة المستخدم

### صفحة التقويم
```
┌─────────────────────────────────────┐
│ التقويم والأوراد                   │
├─────────────────────────────────────┤
│                                     │
│  أحد اثنين ثلاثاء أربعاء خميس جمعة سبت │
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐     │
│  │ 1 │ │ 2 │ │ 3 │ │ 4 │ │ 5 │     │
│  │   │ │   │ │1-10│ │   │ │🌟 │   │
│  └───┘ └───┘ └───┘ └───┘ └───┘     │
│                                     │
└─────────────────────────────────────┘

إحصائيات الشهر:
- إجمالي الأوراد: 20
- أيام العطل: 4
- المجموع: 24
```

### صفحة متابعة الأوراد
```
┌──────────────────────────────────────┐
│  متابعة الأوراد                     │
├──────────────────────────────────────┤
│                                      │
│  [الشعراء 1-10] [الشعراء 11-20] ... │
│                                      │
│  إحصائيات الورد المختار:            │
│  معدل الإكمال: 85% | مكتمل: 12     │
│  قيد الإنجاز: 2 | لم يبدأ: 1       │
│                                      │
│  ┌────────────────────────────────┐  │
│  │     أعمدة بيانية                │  │
│  │     (Bar Chart)                 │  │
│  └────────────────────────────────┘  │
│                                      │
│  الجدول التفصيلي:                   │
│  ┌─────────────────────────────────┐ │
│  │الطالب │مكتمل│الهدف│النسبة│الحالة│ │
│  ├─────────────────────────────────┤ │
│  │أحمد   │ 5  │  5 │ 100%│ ✓     │ │
│  │فاطمة  │ 3  │  5 │ 60% │ ⏳    │ │
│  │علي    │ 0  │  5 │  0% │ ✗     │ │
│  └─────────────────────────────────┘ │
└──────────────────────────────────────┘
```

---

## 💾 التخزين

### محلي (localStorage)
- `wirdAssignments`: جميع الأوراد المضافة
- `wirdProgress`: تقدم الطلاب في كل ورد

**ملاحظة**: في الإنتاج، سيتم حفظ البيانات في Firebase

### Firebase (للمستقبل)
```
/assignments/{year}/{month}/{day}/{wirdId}
/progress/{wirdId}/{studentId}
```

---

## 🔌 الخدمات الرئيسية

### `calendarService.ts`

#### الدوال الأساسية

```typescript
// إنشاء ورد جديد
createWirdAssignment(
  date: string,
  surahId: number,
  startAyah: number,
  endAyah: number,
  targetRepetitions: number
)

// التحقق من صحة الآيات
validateAyahs(surahId, startAyah, endAyah)

// الحصول على معلومات السورة
getSurahInfo(surahId)

// تنسيق اسم الورد
formatWirdName(wird: WirdAssignment)

// إنشاء شبكة التقويم
generateCalendarGrid(year, month)
```

---

## 📱 الاستجابة

- ✅ **الهاتف (320px-768px)**
  - تقويم محسّن لشاشات صغيرة
  - نموذج ملء الشاشة
  - أعمدة بيانية متكيفة

- ✅ **الويب (769px+)**
  - تقويم كامل الحجم
  - نموذج في نافذة منفصلة
  - رسوم بيانية عالية الدقة

---

## 🚀 الخطوات التالية

### مرحلة 2:
1. ✅ حفظ الأوراد في Firebase
2. ✅ المزامنة الفورية بين الأجهزة
3. ✅ تنبيهات الطلاب بالأوراد الجديدة

### مرحلة 3:
1. ✅ تقارير شهرية وسنوية
2. ✅ تصدير البيانات (PDF/Excel)
3. ✅ تحليل متقدم للأداء

### مرحلة 4:
1. ✅ جدولة تلقائية للأوراد
2. ✅ نمط متكرر الأوراد
3. ✅ توزيع ذكي للسور

---

## 📚 الأمثلة

### مثال 1: إضافة ورد ليوم واحد
```
التاريخ: 2026-02-03
السورة: البقرة (رقم 2)
الآيات: 1-20
التكرارات: 7
النتيجة: يظهر "البقرة 1-20" في التقويم
```

### مثال 2: تحديد يوم عطلة
```
التاريخ: 2026-02-06 (جمعة)
اختر: "هذا يوم عطلة"
النتيجة: يظهر 🌟 في التقويم
```

### مثال 3: متابعة ورد
```
اختر "سورة الشعراء 1-10"
انظر:
- أحمد: 5/5 (100%) ✓
- فاطمة: 3/5 (60%) ⏳
- علي: 0/5 (0%) ✗
```

---

## ⚙️ الإعدادات

### عدد السور المدعومة
```
30 سورة (من الفاتحة إلى الناس)
يمكن توسيعها إلى 114 سورة حسب الحاجة
```

### معلومات السورة
```
- الرقم
- الاسم الإنجليزي
- الاسم العربي
- عدد الآيات الكلي
```

---

## 🎯 الدقة العالية

✅ **دقة في التتبع**
- كل ورد معرّف فريد
- كل طالب له سجل منفصل
- تتبع دقيق للتكرارات

✅ **دقة في الحسابات**
- معدل إكمال دقيق
- نسب مئوية صحيحة
- إحصائيات فورية

✅ **دقة في الواجهة**
- أيقونات واضحة
- ألوان معبّرة
- تعليقات توضيحية

---

## 📝 ملاحظات

- البيانات محفوظة محلياً حالياً
- سيتم نقلها لـ Firebase قريباً
- يمكن حذف ورد بالنقر على X
- يمكن تعديل ورد بحذفه وإضافة واحد جديد

---

**آخر تحديث**: فبراير 2026  
**الحالة**: ✅ نسخة 1.0 جاهزة  
**الإصدار**: v1.0.0

