# 📱 نظام متابعة الأوراد - Sheikh Ibrahim Wird Tracking System

## 🎯 نظرة عامة

نظام متقدم لمتابعة حفظ القرآن الكريم لطلاب الشيخ إبراهيم مراد. يوفر النظام تتبعاً دقيقاً للأوراد اليومية من الطلاب مع لوحة تحكم شاملة للشيخ.

---

## ✨ الميزات الرئيسية

### 🎓 لوحة الطالب
- ✅ عداد حي للأوراد اليومية
- ✅ تتبع 7 أيام الأخيرة
- ✅ عرض التقدم والهدف
- ✅ إشعارات عند إكمال الورد
- ✅ سجل الإنجازات الشخصي

### 👨‍🏫 لوحة الشيخ
- ✅ **متابعة شاملة**: عرض تقدم جميع الطلاب في لحظة واحدة
- ✅ **إحصائيات فورية**: 
  - عدد الطلاب المكتملين اليوم
  - متوسط الإكمال
  - نسبة الاستمرارية
  - إجمالي التكرارات

- ✅ **بطاقات تقدم الطلاب**:
  - حالة الورد (مكتمل / قيد الإنجاز / لم يبدأ)
  - نسبة الإنجاز المئوية
  - آخر تحديث

- ✅ **صفحة التفاصيل**: لكل طالب
  - إحصائيات الأسبوع والشهر
  - رسوم بيانية للأداء
  - ملاحظات الأداء

- ✅ **صفحة الإحصائيات**:
  - ترتيب الأفضل أداءً
  - الأكثر التزاماً
  - جدول شامل لجميع الطلاب

---

## 🏗️ بنية المشروع

```
src/
├── app/
│   ├── page.tsx              # الصفحة الرئيسية
│   ├── login/                # تسجيل الدخول
│   ├── stats/                # الإحصائيات (للشيخ)
│   └── profile/
│       └── [studentId]/      # صفحة تفاصيل الطالب (للشيخ)
│
├── components/
│   ├── StudentDashboard.tsx  # لوحة الطالب
│   ├── SheikhDashboard.tsx   # لوحة الشيخ
│   ├── WirdProgressCard.tsx  # بطاقة تقدم الورد
│   ├── RepetitionCounter.tsx # العداد الدائري
│   ├── Navigation.tsx        # التنقل
│   └── ...
│
├── hooks/
│   ├── useRealtime.tsx       # إدارة البيانات في الوقت الفعلي
│   └── useWird.tsx           # إدارة الأوراد
│
├── services/
│   └── progressService.ts    # خدمات حساب الإحصائيات
│
└── types/
    └── index.ts              # أنواع البيانات (TypeScript)
```

---

## 📊 أنواع البيانات

### StudentData
```typescript
interface StudentData {
  uid: string;
  name: string;
  email: string;
  phone?: string;
  progress: number;
  status: string;
  lastUpdated: number;
  joinedDate?: number;
  dailyProgress: Record<string, number>; // YYYY-MM-DD -> count
  weeklyStats?: Record<string, WeeklyStats>;
  monthlyStats?: Record<string, MonthlyStats>;
}
```

### WirdProgress
```typescript
interface WirdProgress {
  studentId: string;
  studentName: string;
  date: string;
  currentCount: number;
  targetCount: number;
  percentage: number;
  status: "completed" | "in-progress" | "not-started";
  lastUpdate: number;
}
```

---

## 🔄 تدفق البيانات

```
الطالب يقوم بتحديث التكرارات
         ↓
useRealtime.updateProgress()
         ↓
Firebase Database Update
         ↓
students/{uid}/dailyProgress/{date}
         ↓
الشيخ يرى التحديث فوراً في لوحة التحكم
```

---

## 🚀 كيفية الاستخدام

### للطالب:
1. قم بتسجيل الدخول بحسابك
2. اختر اليوم المطلوب من الأسفل
3. انقر على العداد الدائري لزيادة التكرارات
4. عند الوصول إلى الهدف، تظهر رسالة تهنئة

### للشيخ:
1. قم بتسجيل الدخول بحساب الشيخ
2. ستظهر لوحة التحكم تلقائياً
3. عرض تقدم جميع الطلاب
4. انقر على أي طالب لعرض تفاصيله
5. اذهب لصفحة الإحصائيات للتقارير المتقدمة

---

## 🔐 الأمان والأدوار

### الأدوار:
- **الشيخ**: `admin00@gmail.com` → دخول كامل لجميع البيانات
- **الطالب**: أي بريد آخر → دخول محدود لبيانات شخصية فقط

### التحقق:
- تتم المصادقة عبر Firebase Authentication
- يتم التحقق من الدور عند كل عملية
- البيانات محمية بقواعد Firebase Security Rules

---

## 📈 الإحصائيات المحسوبة

### المستوى اليومي:
- عدد التكرارات المكتملة
- النسبة المئوية للإنجاز
- الحالة (مكتمل/قيد الإنجاز/لم يبدأ)

### المستوى الأسبوعي:
- إجمالي التكرارات
- عدد الأيام المكتملة
- المتوسط اليومي
- أفضل يوم

### المستوى الشهري:
- إجمالي التكرارات
- عدد الأيام المكتملة
- المتوسط اليومي
- نسبة التحسّن

---

## 🛠️ خدمات المساعدة

### `progressService.ts`
يوفر دوال مساعدة لحساب الإحصائيات:

```typescript
// حساب إحصائيات الأسبوع
calculateWeeklyStats(dailyProgress, date)

// حساب إحصائيات الشهر
calculateMonthlyStats(dailyProgress, date)

// حساب نسبة الاكتمال
calculateCompletionRate(current, target)

// الحصول على درجة الأداء
getPerformanceGrade(completionRate, consistency)
```

---

## 🔌 التكامل مع Firebase

### Realtime Database Structure:
```
firebase/
├── students/
│   ├── {uid}/
│   │   ├── name
│   │   ├── email
│   │   ├── dailyProgress
│   │   │   ├── 2025-02-07: 100
│   │   │   ├── 2025-02-06: 100
│   │   │   └── ...
│   │   └── ...
│
├── assignments/
│   ├── 2025-02-07/
│   │   ├── surahName: "الفرقان"
│   │   ├── repetitions: 100
│   │   └── ...
│   └── ...
```

---

## 🎨 التصميم والواجهة

- **Framework**: Next.js 15
- **Styling**: Tailwind CSS v4
- **Font**: أمیری (للعربية) + IBM Plex Sans Arabic
- **Icons**: Lucide React
- **Theme**: Dark Mode (متقدم)

---

## ⚡ الأداء والتحسينات

### تحسينات الأداء:
- ✅ استخدام `useMemo` لتخزين الحسابات
- ✅ تحديثات فورية عبر Firebase Realtime
- ✅ تحميل كسول (Lazy Loading)
- ✅ تخزين محلي للبيانات المتكررة

### تحسينات التجربة:
- ✅ رسوم متحركة سلسة
- ✅ ردود فعل فورية (Haptic Feedback)
- ✅ تنبيهات واضحة
- ✅ واجهة سهلة الاستخدام

---

## 📋 المتطلبات والتثبيت

```bash
# تثبيت المتطلبات
npm install

# تشغيل المشروع
npm run dev

# بناء للإنتاج
npm run build

# بدء الإنتاج
npm start
```

---

## 🔮 المميزات المستقبلية

- [ ] إشعارات البريد الإلكتروني
- [ ] تطبيق الهاتف المحمول
- [ ] نظام الشارات والجوائز
- [ ] تصدير التقارير (PDF/Excel)
- [ ] منتدى نقاش الطلاب
- [ ] مكتبة الموارد التعليمية
- [ ] اختبارات دورية

---

## 📞 الدعم والمساعدة

لأي استفسارات أو مشاكل تقنية، يرجى التواصل مع فريق التطوير.

---

**تم التطوير بواسطة**: فريق التطوير  
**آخر تحديث**: فبراير 2025
